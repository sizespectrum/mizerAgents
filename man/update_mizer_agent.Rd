% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_mizer_agent.R
\name{update_mizer_agent}
\alias{update_mizer_agent}
\title{Refresh a project's mizer agent files, keeping its settings}
\usage{
update_mizer_agent(path = ".", check_version = TRUE, ...)
}
\arguments{
\item{path}{Directory to refresh. Defaults to the current working directory,
which should be your R project root.}

\item{check_version}{Logical; whether to check if a newer version of
\code{mizerAgents} is available on GitHub. Defaults to \code{TRUE}.}

\item{...}{Arguments passed on to \code{\link[=setup_mizer_agent]{setup_mizer_agent()}}, overriding the
detected settings. \code{path = "."}, for instance, is refreshed with
\code{update_mizer_agent(run_r = FALSE)} if you want to turn code execution off
while refreshing.}
}
\value{
Invisibly, the path to the \code{AGENTS.md} file, as
\code{\link[=setup_mizer_agent]{setup_mizer_agent()}} returns.
}
\description{
Re-runs \code{\link[=setup_mizer_agent]{setup_mizer_agent()}} with the options this project was set up with,
rather than with the defaults for a new one. Use it to pick up a new version
of the skills or of the reference card - after upgrading mizer, say, since
the skills come from the installed mizer - without having to remember how the
project was configured.
}
\details{
The files themselves are refreshed exactly as \code{setup_mizer_agent()} refreshes
them, because that is what does the work: the reference card is rewritten,
the marked block in each instruction file is updated in place with your own
notes left alone, and the skills are refreshed file by file, keeping and
reporting any that have been edited here. What this function adds is that
your \emph{settings} survive.

\code{setup_mizer_agent()} is declarative: its arguments describe the setup you
want, and its defaults are the ones for a fresh project. Re-running it plainly
therefore re-declares them - switching code execution back on in a project
that had turned it off with \code{run_r = FALSE}, dropping the package tools from
one set up with \code{pkg_dev = TRUE}, and writing config files for agents you had
narrowed away from with \code{agents}. This function reads those choices back from
what the last run wrote and replays them, so a refresh changes only content.
(\code{setup_mizer_agent()} now reports any setting it changes, so a plain re-run
at least says what it did.)

Nothing is stored to make this work: \code{r_session}, \code{run_r}, \code{pkg_dev} and
\code{agents} are read from the MCP configs, and \code{rprofile} from the project
\code{.Rprofile}, which means it works for projects set up by earlier versions of
this package too. The one choice that leaves no trace is \code{agents = "copilot"},
which only prints a snippet; pass it again yourself if you want it. Pass any
argument of \code{setup_mizer_agent()} through \code{...} to override what was detected.
}
\examples{
\dontrun{
# After upgrading mizer, refresh the skills without changing the setup
update_mizer_agent()
}
}
\seealso{
\code{\link[=setup_mizer_agent]{setup_mizer_agent()}} to set a project up, and
\code{\link[=remove_mizer_agent]{remove_mizer_agent()}} to undo it.
}
