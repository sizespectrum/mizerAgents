% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup_mizer_agent.R
\name{setup_mizer_agent}
\alias{setup_mizer_agent}
\title{Set up an AI agent to help with your mizer project}
\usage{
setup_mizer_agent(
  path = ".",
  overwrite = FALSE,
  r_session = TRUE,
  run_r = TRUE,
  pkg_dev = FALSE,
  rprofile = FALSE,
  agents = .agent_choices
)
}
\arguments{
\item{path}{Directory in which to create or update the agent files. Defaults
to the current working directory, which should be your R project root.}

\item{overwrite}{If \code{TRUE}, replace existing \code{AGENTS.md}, \code{CLAUDE.md} and
\code{GEMINI.md} files entirely with a clean shim, discarding your project
notes. If \code{FALSE} (the default), keep the rest of each file and only
refresh the marked mizer block, adding it at the top if it is not there
yet. \code{MIZER-AGENTS.md} is always overwritten to ensure it stays up-to-date.}

\item{r_session}{If \code{TRUE} (the default), configure the \code{r-mizer} MCP server
so that the agent can read mizer's documentation, your global environment
and the open RStudio document. Set to \code{FALSE} to write no MCP config at
all.}

\item{run_r}{If \code{TRUE} (the default), let the agent \emph{evaluate} R code in your
session, which is what allows it to project or calibrate a model and see
the resulting plots. The code runs in your global environment with no
sandboxing, so the agent can overwrite your objects; work under version
control, or set this to \code{FALSE} for a read-only connection. Ignored when
\code{r_session = FALSE}.}

\item{pkg_dev}{If \code{TRUE}, also expose btw's package development tools
(\code{load_all()}, \code{document()}, \code{test()}, \code{check()} and test coverage), which
run against the package in your session's working directory. Set this when
the project is itself an R package, such as a mizer extension. \code{FALSE} by
default, since these tools do nothing useful in an ordinary modelling
project. Ignored when \code{r_session = FALSE}.}

\item{rprofile}{If \code{TRUE}, append a guarded \code{btw::btw_mcp_session()} call to
the project \code{.Rprofile}, so that each new session hands itself to the MCP
server automatically. \code{FALSE} by default, in which case you call
\code{btw::btw_mcp_session()} yourself. Ignored when \code{r_session = FALSE}.}

\item{agents}{Which agents to configure the server for. By default all of
them, since the config files are small, sit in different places and do not
interfere with each other, so a project set up on your machine works for a
collaborator using a different agent. Any of:\tabular{ll}{
   Value \tab File written \cr
   \code{"claude"} \tab \code{.mcp.json} \cr
   \code{"codex"} \tab \code{.codex/config.toml} \cr
   \code{"gemini"} \tab \code{.gemini/settings.json} \cr
   \code{"antigravity"} \tab \code{.agents/mcp_config.json} \cr
   \code{"cursor"} \tab \code{.cursor/mcp.json} \cr
   \code{"vscode"} \tab \code{.vscode/mcp.json} \cr
   \code{"posit"} \tab \code{.posit/assistant/settings.json} \cr
   \code{"copilot"} \tab \emph{(none; instructions printed)} \cr
}


\code{"posit"} covers Posit Assistant, which runs in RStudio as well as
Positron. Copilot CLI reads MCP config only from the user-wide
\verb{~/.copilot/mcp-config.json}, so nothing is written for it; you get the
snippet to paste there instead. Ignored when \code{r_session = FALSE}.}
}
\value{
Invisibly returns the path to the \code{AGENTS.md} file.
}
\description{
Creates (or updates) a \code{MIZER-AGENTS.md} file in your project directory
containing a concise mizer reference that AI coding agents read
automatically on startup. The file includes the core mizer workflow, key
object descriptions, and the path to the bundled API index: a curated list
of every exported mizer function, grouped by workflow stage, for finding the
right function. Argument lists are deliberately not bundled: those go stale
silently, so the card sends agents to the help pages of the mizer version you
actually have installed.
}
\details{
It also creates (or updates) the instruction files agents read at startup -
\code{AGENTS.md}, \code{CLAUDE.md} and \code{GEMINI.md} - adding to each a short note and a
\verb{@MIZER-AGENTS.md} import, so that agents read both the project-specific
instructions and the mizer reference. Agents that resolve \code{@} imports (Claude
Code, Gemini CLI) pick the reference up automatically at startup; the note
tells those that do not (Codex, Copilot) to read the file themselves. All
three files are handled alike, because none of them is a fallback for
another: Claude Code reads \code{CLAUDE.md} and not \code{AGENTS.md}, and Gemini CLI
reads \code{GEMINI.md}, so an agent that looks only for its own named file still
finds the block. The block is delimited by \verb{<!-- mizerAgents: start -->} and
\verb{<!-- mizerAgents: end -->} comments and is refreshed in place on every run,
so that improvements to it reach existing projects. Add your own project
notes outside those markers, where they will be left untouched.

The block is the only thing added: no \verb{@AGENTS.md} import is ever written or
removed, so which of your own instructions reach which agent is unchanged. If
one of these files already imports \code{AGENTS.md}, whether you wrote that
yourself or an earlier version of this package did, it is left untouched -
the block reaches the agent through the import.

It also installs a set of Claude Code \emph{skills} into \verb{.claude/skills/} (one
sub-directory with a \code{SKILL.md} per skill, e.g. \code{analyse-and-plot} and
\code{build-multispecies-model}). Claude Code loads these automatically when a
task matches, giving step-by-step guidance for common mizer workflows. Like
\code{MIZER-AGENTS.md}, the skills are package-managed and refreshed on every call
so they stay up to date.

The skills are taken from the \strong{installed mizer} (\verb{inst/skills/}), not from
this package, so the guidance an agent follows always describes the mizer the
project is actually running. In mizer each \code{SKILL.md} is also the source of
the matching \verb{cheatsheet-*} article on the mizer website, so the two are the
same document. Skills arrived in mizer 3.2.2; against an older mizer this
function still writes everything else and reports that it installed none.

What a project learns about mizer is kept separate from them, so that neither
overwrites the other. Each skill's directory may hold a \code{NOTES.md}, which this
package never writes and which the \code{SKILL.md} tells agents to read alongside
it and to treat as taking precedence: that is where an agent should record
what it discovers about \emph{your} model. Findings that are true of mizer in
general belong upstream instead, and the skills tell agents to offer to report
them at \url{https://github.com/sizespectrum/mizerAgents/issues}, so that every
project gets them in the next release.

Refreshing works file by file rather than by replacing whole directories, so
a \code{NOTES.md}, or a skill of your own, is left alone. The hashes of the files
installed are recorded in \code{.claude/skills/.mizerAgents.json}; a file that has
since been edited is recognised, kept, and reported, with the new version
written beside it as \verb{<file>.new} for you to merge, rather than silently
overwritten. (A project set up by version 0.3.2 or earlier has no such record,
so the first run after upgrading refreshes the skills as it always did and
starts keeping one.) Files that later versions stop shipping are removed if
they are unmodified.

So that agents other than Claude Code (which do not discover \verb{.claude/skills/}
natively) can use the skills too, an index of them (each skill's
name, one-line description, and path) is generated from the skills' own frontmatter
and added to \code{MIZER-AGENTS.md}. Those agents can then read the relevant
\code{SKILL.md} on demand when a task matches.

Finally, unless \code{r_session = FALSE}, it configures an MCP server named
\code{r-mizer} so that the agent can reach your live R session. Agents have not
converged on where MCP servers are configured, so by default every one that
supports a project-level config gets its own file: Claude Code, Codex,
Gemini CLI, Antigravity, Cursor, VS Code and Posit Assistant. See the
\code{agents} argument for the paths and for how to narrow the list.
The server is provided by the
\href{https://posit-dev.github.io/btw/}{btw} package, which you need to install
separately. This gives the agent help pages and vignettes for the mizer
version you actually have installed, rather than whatever mizer API it
remembers, along with a view of the objects in your global environment and
of the document open in RStudio, and it can run mizer code there and see the
plots that come back. For the server to see your session you must run
\code{btw::btw_mcp_session()} in the RStudio console; passing \code{rprofile = TRUE}
adds that call to the project \code{.Rprofile} so that it happens automatically.
Only the \code{r-mizer} entry of \code{.mcp.json} is package-managed; other servers you
configure there are left alone.

After running this function, start your AI coding agent
(e.g. \code{claude}, \code{codex}, \code{copilot} or \code{gemini}) from the RStudio Terminal
and it will immediately have the mizer context it needs.
}
\examples{
\dontrun{
# Run once in your mizer project to set up AI agent support
setup_mizer_agent()

# In a mizer extension package, add the package development tools and
# connect the session automatically on startup
setup_mizer_agent(pkg_dev = TRUE, rprofile = TRUE)

# A read-only connection: documentation and inspection, but no execution
setup_mizer_agent(run_r = FALSE)
}
}
\seealso{
\code{\link[=remove_mizer_agent]{remove_mizer_agent()}}, which undoes all of this.
}
