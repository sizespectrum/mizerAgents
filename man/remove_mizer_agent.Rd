% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove_mizer_agent.R
\name{remove_mizer_agent}
\alias{remove_mizer_agent}
\title{Undo what \code{setup_mizer_agent()} did}
\usage{
remove_mizer_agent(path = ".")
}
\arguments{
\item{path}{Directory to clean up. Defaults to the current working
directory, which should be your R project root.}
}
\value{
Invisibly, a character vector of the paths that were removed or
changed.
}
\description{
Removes the mizer agent support that \code{\link[=setup_mizer_agent]{setup_mizer_agent()}} installed in a
project: the \code{MIZER-AGENTS.md} reference card, the mizer block in the
instruction files, the bundled skills in \verb{.claude/skills/}, the \code{r-mizer} MCP
server in each agent's config, and the \code{btw::btw_mcp_session()} call in the
project \code{.Rprofile}. Use it when you no longer want the agent support in a
project, or to get back to a clean slate before setting it up differently.
}
\details{
Only what this package wrote is removed, following the same boundary
\code{setup_mizer_agent()} respects when writing:
\itemize{
\item \code{MIZER-AGENTS.md} is deleted. It is package-managed and rewritten on every
setup, so there is nothing of yours in it.
\item In \code{AGENTS.md}, \code{CLAUDE.md} and \code{GEMINI.md} only the block between the
\verb{<!-- mizerAgents: start -->} and \verb{<!-- mizerAgents: end -->} markers is
deleted; your own notes stay. A file that held nothing but the block is
deleted with it. An \verb{@AGENTS.md} import is never removed, just as it is
never written: which of your instruction files import which is your
business.
\item Under \verb{.claude/skills/}, a file is removed only if it still matches the
hash recorded in \code{.claude/skills/.mizerAgents.json} when it was installed.
One that has been edited here is kept and reported, as is any \code{NOTES.md},
which this package never writes. Directories left empty are removed. (In a
project set up by version 0.3.2 or earlier there is no record, so files are
identified by comparing them with what the installed mizer's skills would
have produced; anything that does not match is kept.)
\item In each agent's MCP config only the \code{r-mizer} entry is removed; other
servers you configured there stay, and the file is deleted only if that
entry was all it held. A config file that cannot be parsed is left alone
with a warning rather than risking your other servers.
\item In \code{.Rprofile} only the guarded \code{btw::btw_mcp_session()} call and its
comment are removed.
}

The \code{btw} package is not touched: it is an ordinary R package you installed,
and other projects may be using it. Uninstall it yourself with
\code{remove.packages("btw")} if you want it gone.
}
\examples{
\dontrun{
# Remove the mizer agent support from the current project
remove_mizer_agent()
}
}
\seealso{
\code{\link[=setup_mizer_agent]{setup_mizer_agent()}}, which this undoes.
}
